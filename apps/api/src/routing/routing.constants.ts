/**
 * Token de injeção de dependência para o provedor de roteamento ativo.
 */
export const ROUTING_PROVIDER = Symbol('ROUTING_PROVIDER');

/**
 * Token de injeção de dependência para as opções de configuração do roteamento.
 */
export const ROUTING_OPTIONS = Symbol('ROUTING_OPTIONS');
